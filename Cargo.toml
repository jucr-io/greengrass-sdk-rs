[package]
name = "greengrass-sdk-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
cxx = "1.0"
tokio = { version = "1", features = ["rt", "sync"] }
tracing = "0.1"

[build-dependencies]
cxx-build = "1.0"
cmake = "0.1"
